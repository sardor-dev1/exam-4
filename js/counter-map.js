let loading = document.querySelector(".loading");

function loadingMore() {
  let loading__card = document.createElement("div");
  loading__card.className = "loading__card";
  loading.appendChild(loading__card);

  if (loading__card.length + loading__card === loading) {
    loading.innerHTML = "Welcome";
  }
}
